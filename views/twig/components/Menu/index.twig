<menu id="main-menu" class="hw-main-menu">
  <div class="hw-main-menu__inner">
    {% for item in menus.main.items %}
      <a href="{{ item.link }}" class="hw-main-menu__link {{ item.classes|join(' ') }}">{{ item.title }}</a>
    {% endfor %}

    <!-- {{ function('do_action', 'wpml_add_language_selector') }} -->
  </div>
</menu>
