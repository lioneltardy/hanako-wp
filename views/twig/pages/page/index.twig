{% extends 'structure/html.twig' %}

{% block content %}
  <main class="py-16 lg:py-32">
    <div class="container">
      <h1 class="font-headline text-large">{{ post.title }}</h1>
      <div class="text">
        {{ post.content }}
      </div>
    </div>
  </main>
{% endblock %}
