{% extends 'structure/html.twig' %}

{% block content %}
  <div class="container">
    <h1>{{ post.title }}</h1>
    {{ post.content }}
    {{ function('comments_template') }}
  </div>
{% endblock %}
